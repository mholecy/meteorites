<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:foreground="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true">

    <TextView
            android:id="@+id/tv_meteorite_item_title_java"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceListItem"
            android:layout_marginEnd="8dp"
            android:gravity="bottom"
            android:layout_marginStart="8dp"
            android:layout_marginTop="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            tools:text="Meteorite"/>

    <TextView
            android:layout_width="0dp"
            android:layout_height="20dp"
            android:id="@+id/tv_meteorite_mass_java"
            android:layout_marginEnd="8dp"
            app:layout_constraintStart_toStartOf="@+id/tv_meteorite_item_title_java"
            app:layout_constraintTop_toBottomOf="@+id/tv_meteorite_item_title_java"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="2,5 kg"/>

</androidx.constraintlayout.widget.ConstraintLayout>

